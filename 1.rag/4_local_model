# 加载模型
from sentence_transformers import SentenceTransformer

# 加载本地模型   嵌入模型  把数据转换成向量
model = SentenceTransformer(r'C:\dev\llm\local-model\bge-large-zh-v1.5')

data = ['你好']
# 默认返回的nump数组
embedding = model.encode(data)
print(embedding.shape)
